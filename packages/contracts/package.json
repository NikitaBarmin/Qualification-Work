{
  "name": "@businesspulse/contracts",
  "version": "0.1.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "oxlint -c ../../oxlint.config.js .",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "node -e \"console.log('contracts package has no runtime tests yet')\"",
    "clean": "node -e \"require('node:fs').rmSync('dist', { recursive: true, force: true }); require('node:fs').rmSync('.turbo', { recursive: true, force: true });\""
  },
  "devDependencies": {
    "prettier": "^3.8.1",
    "typescript": "^5.9.3"
  }
}
